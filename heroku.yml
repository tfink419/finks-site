build:
  docker:
    flask: flask/Dockerfile
    browserify: browserify/Dockerfile
release:
  command:
    - browserify -v src/main.jsx -o dist/bundle.js
  image: browserify
run:
  web:
    command: python /home/main.py
    image: flask
