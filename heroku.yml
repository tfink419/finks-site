build:
  docker:
    flask: flask/Dockerfile
    browserify: browserify/Dockerfile
release:
  command:
    - browserify -v ./src/jsx/main.jsx -o ./src/python/static/js/bundle.js
  image: browserify
run:
  web:
    command:
    - python ./src/python/main.py
    image: flask
